function dontAskWhy(arr) {
  const words = ['sky', 'hot', 'Yeti', 'green'];
   for(let i = 0; i < arr; i++);
   //I know I need to do something that finds y's in words but...findIndex? filter? augh
   
}