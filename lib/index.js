function dontAskWhy(words) {
  const words = ['sky', 'hot', 'Yeti', 'green'];
   for(let i = 0; i < arr; i++);
   //I know I need to do something that finds y's in words but...find? filter? mapped? split something? augh
}
return;
